accelerator: GPU
gpus: 1
precision: 16
max_epochs: 10
deterministic: true
log_every_n_steps: 100
# Validation
check_val_every_n_epoch: 1000  # Disable validation
# Checkpointing
checkpoint_callback: null  # Disable checkpointing
enable_checkpointing: false  # Disable checkpointing
default_root_dir: ${general.model_dir}
# Utilities
num_sanity_val_steps: 0